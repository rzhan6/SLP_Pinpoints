Cambiar el tamaño de la imagen y convertir la imagen to grayscale.
Desde un profundidad de 24 bits (capturado con el SW propio de Heimann) a 8 bits 
que se simila al imagen IR de base de datos SLP. (hay que tener cuidado con los bodes de la imagen capturada, se tiene que recortarlo.)